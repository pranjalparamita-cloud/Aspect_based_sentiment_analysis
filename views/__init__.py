"""
views/ — One file per app page (screen).
---------------------------------------
login.py     Login / Sign-Up / Google account chooser
analysis.py  product search grid + NLP dashboard
profile.py   edit username, password, photo, bio
history.py   user's past analyses
admin.py     admin-only user tracking panel

(Named `views/` instead of `pages/` so Streamlit does NOT auto-add them
to the sidebar — navigation stays behind our login gate in app.py.)
"""
